apiVersion: v1
kind: Service
metadata:
  name: whisper
  namespace: whisper
  annotations:
    # Next free slot after .240 (argocd), .241 (ollama), .242 (grafana),
    # .243 (ingress-nginx), .244 (ollama-chat).
    metallb.io/loadBalancerIPs: "192.168.1.245"
spec:
  type: LoadBalancer
  selector:
    app: whisper
  ports:
    - port: 9000
      targetPort: 9000
